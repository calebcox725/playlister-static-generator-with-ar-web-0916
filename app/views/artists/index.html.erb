<html>
  <head>
    <title>Artists</title>
  </head>
  <body>
    <ul>
      <% Artist.all.each do |artist| %>
      <li><a href="/artists?id=<%= artist.id %>">
        <%= artist.name %>
      </a></li>"
      <% end %>
    </ul>
  </body>
</html>